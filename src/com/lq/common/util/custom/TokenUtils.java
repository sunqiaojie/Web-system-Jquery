package com.lq.common.util.custom;

import java.security.NoSuchAlgorithmException;
import java.util.HashMap;
import java.util.Map;

import com.lq.common.util.plugin.MD5HashUtil;

public class TokenUtils {

	/*private static Map<String, User> onlineUsers = new HashMap<String, User>();
	
	public static String generateToken(String str) {
		try {
			return MD5HashUtil.hashCode(str);
		} catch (NoSuchAlgorithmException e) {
			e.printStackTrace();
		}
		return null;
	}*/
	
/*	public static User getUser(String token) {
		return onlineUsers.get(token);
	}
	
	public static boolean verifyToken(String token) {
		return onlineUsers.containsKey(token);
	}
	
	public static void saveOnlineUser(String token, User user) {
		onlineUsers.put(token, user);
	}

	public static void removeOnlineUser(String token) {
		onlineUsers.remove(token);
	}*/
	
}
